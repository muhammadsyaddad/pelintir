---
description: Audit changed Indonesian user-facing copy against the editorial policy
---

Run the `procurement-copy-review` skill against the working diff (`git diff` plus untracked files).

If `$ARGUMENTS` names a path or a flag id, scope the review to that instead.
