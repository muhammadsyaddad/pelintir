---
description: Create an ADR with correct numbering and update the index
---

Run the `write-adr` skill for: $ARGUMENTS

If no title is given, summarise the working diff and propose whether it contains a decision that needs a record
— then stop and ask, rather than writing one speculatively.
