# Copy to .env and adjust. Never commit .env.

# --- Postgres (docker-compose) ---
POSTGRES_USER=pelintir
POSTGRES_PASSWORD=pelintir
POSTGRES_DB=pelintir
POSTGRES_PORT=5432

# --- Meilisearch (docker-compose) ---
# Development value. Generate a real key before deploying:
#   openssl rand -base64 32
MEILI_MASTER_KEY=xLcOLLdqj/TJ3JhviVEyyTU/vDfTTVJqF7Tb3Oi+A80=
MEILI_PORT=7700

# --- API (apps/api) ---
DATABASE_URL=postgresql://pelintir:pelintir@localhost:5432/pelintir
MEILI_URL=http://localhost:7700
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001"]

# --- Pipeline (packages/pipeline) ---
# Local path for now. Becomes s3://<bucket> when raw storage moves to R2.
PELINTIR_DATA_DIR=./data
